export enum NotifyTypes {
  VOTE = 1,
  MENTION = 2,
  FOLLOW = 3,
  COMMENT = 4,
  RE_BLOG = 5,
  TRANSFERS = 6,
  FAVORITES = 13,
  BOOKMARKS = 15,
  ALLOW_NOTIFY = "ALLOW_NOTIFY"
}

export enum NotificationViewType {
  ALL = "All",
  UNREAD = "Unread",
  READ = "Read"
}
