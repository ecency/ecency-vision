import { MarketAdvancedModeWidget } from "./market-advanced-mode-widget";
import React from "react";

export const StakeBuyWidget = () => {
  return <MarketAdvancedModeWidget children={<>Its stake buy</>} />;
};
