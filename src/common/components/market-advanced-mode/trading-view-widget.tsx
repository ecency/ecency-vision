import { MarketAdvancedModeWidget } from "./market-advanced-mode-widget";
import React from "react";

export const TradingViewWidget = () => {
  return <MarketAdvancedModeWidget children={<>Its trading view</>} />;
};
